import React, { Component } from "react";

export default class Disciplines extends Component {
    render() {
        return (
            <div>
                Disciplines
            </div>
        )
    }
}